# Stream API
![[Stream API.png]]
<- 중간 연산 과정(중간 연산 메소드들의 리턴은 Stream이다.) ->

| Collection<br>Map<br>Array | Stream | filter<br>요소를 걸러내는 과정 | sorted<br>요소를 정렬 | map<br>Strea, 요소를 다른 요소로 변환 | ....... | collect |
| -------------------------- | ------ | --------------------- | ---------------- | --------------------------- | ------- | ------- |
- Stream은 파이프라인으로 연결되어 중간연산과정은 n번을 진행하고, 최종 연산은 1번 진행
- 반드시 최종 연산이 있어야 한다.
- 한번 사용한 Stream은 다시 사용할 수 없다. (1회성)
- 원본 데이터를 훼손하지 않는다.
- original .....



### 람다식에서 메소드 참조와 생성자 참조란?
> Java 8에서는 람다식을 도입하면서, 코드가 더욱 간결해졌다. 람다식에서 더 나아가 메소드 참조와 생성자 참조를 사용하면 **기존 메소드를 더욱 직관적으로 사용할 수 있다.**

#### 1. 메소드 참조란?
> 메소드 참조는 **==이미 존재하는 메소드를 람다식보다 더 간결하게 표현하는 방법==**으로, 람다식에서 불필요한 코드를 줄이고, 메소드 자체를 참조해서 호출할 수 있다.

##### 기본 문법
```
클래스 이름 :: 메소드 이름
객체이름 :: 메소드 이름

:: 기호를 사용해서 메소드를 참조한다.
```

###### 메소드 참조의 종류![[표준함수형인터페이스.png]]

| 종류  | 람다식 | 메소드 참조 |
| --- | --- | ------ |
|     |     |        |
|     |     |        |
|     |     |        |
|     |     |        |

###### 메소드 참조 예제
```java

```

![[표준함수형인터페이스.png]]



```java
public class MethodReferenceExample01 {
	public static void main(String[] args) {
	// 기존방식
		Function<Integer, Integer> beforeSquare = new Function<Integer, Integer>() {
			@Override
			public Integer apply(Integer t) {
				return Utils.square(t);
		}
	}; 

// 람다식 방식
Function<Integer, Integer> lambdaSquare = x -> Utils.square(x);
// 메소드 참조 방식
Function methodRefSquare = Utils::square;
	System.out.println(beforeSquare.apply(5)); // 25
	System.out.println(lambdaSquare.apply(5)); // 25
	System.out.println(methodefRSquare.apply(5)); // 25
	} 
}
```

```JAVA
package lamda.methodRef;
import java.util.function.BiFunction;
public class SpecificInstanceMethod03 {
public static void main(String[] args) {

	//기존방식
	BiFunction beforeEquals = new BiFunction() {
	@Override
	public Boolean apply(String t, String u) {
		return t.equals(u);
		}
	};

	// 람다식방식
	BiFunction<String, String, Boolean> lambdaEquals = (s1, s2) -> s1.equals(s2);

	// 메소드 참조방식
		BiFunction methodRefEquals = String::equals;
		
		System.out.println(beforeEquals.apply("heejung", "heejung")); // true
		System.out.println(lambdaEquals.apply("Java", "Java")); // true
		System.out.println(methodRefEquals.apply("Java", "Python")); // false
	}
}
```

#### 2. 생성자 참조란?
```java
package lamda.constructorRef;
import java.util.function.Supplier;

class Person {
	public Person() {
		System.out.println("새로운 Person 객체 생성!");
		}
	}
	
public class ConstructorReference01 {
	public static void main(String[] args) {
	// 기존 방식
		Supplier beforePerson = new Supplier() {
			@Override
			public Person get() {
				return new Person();
				}
			};
	
		// 람다식 방식
		Supplier lambdaPerson = () -> new Person();
	
		// 생성자 참조 방식
		Supplier methodRefPerson = Person::new;
		Person p1 = beforePerson.get(); // 새로운 Person 객체 생성
		Person p2 = lambdaPerson.get(); // 새로운 Person 객체 생성
		Person p3 = methodRefPerson.get(); // 새로운 Person 객체 생성
	}
}
```

