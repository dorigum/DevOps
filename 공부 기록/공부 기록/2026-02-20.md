# Collection
## Set 컬렉션
- Set 컬렉션은 저장 순서가 유지되지 않음
- 객체를 중복해서 저장할 수 없고, 하나의 Null

### HashSet
- 동등 객체를 중복 저장하지 않음
- 다른 객체라도 `hashCode()` 메소드의 리턴 값이 같고, `equals()` 메소드가 true를 리턴하면 동일한 객체라고 판단하고 중복을 저장하지 않음

---
## Map 컬렉션
- 키와 값으로 구성된 엔트리 객체를 저장
- 키는 중복 저장할 수 없지만, 값은 중복 저장할 수 있음
- ==기존에 저장된 키와 동일한 키로 값을 저장하면 새로운 값으로 대치==

### HashMap
### HashTable

```java
// 1. 현재 클래스가 있는 위에서부터 경로를 설정
PropertiesExam.class.getResourceAsStream("info.properties");

// 2.this.getClass() 를 호출하면 classes 폴더를 기준(bin)으로 경로 설정
this.getClass().getClassLoader().getResourceAsStream("a.properties");

// 3. 프로젝트 내 resources 폴더 존재 -> 정적 문서(환경 설정 문서, schema, xml 문서)

// 웹 프로젝트
// 1. back - resources
// 2. front - resources/css, js, img
```

