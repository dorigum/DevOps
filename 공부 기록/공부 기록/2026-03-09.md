# MVC 구조
![[1_94pj0_fUOCE7eIgg2oOaHA.png]]![[Pasted image 20260309092436.png]]

----
# JDBC
## JDBC(Java Database Connectivity) API

### JDBC (Java Database Connectivity)란?
- 자바에서 데이터베이스에 접근하기 위한 표준 API
- 다양한 DBMS(MySQL, Oracle, PostgreSQL 등)와 독립적으로 동작할 수 있게 해줌
- JDBC는 드라이버 매니저(DriverManager)를 통해 데이터베이스 벤더가 제공하는 드라이버를 로드하고, SQL 실행을 가능하게 함.

### JDBC 아키텍처
1. JDBC API
   : DriverManager, Connection, Statement, ResultSet 등의 인터페이스 제공

2. JDBC Driver
   : DB 벤더(예: MySQL, Oracle)가 제공하는 실제 구현체

3. 드라이버 종류
   Type 1: JDBC-ODBC Bridge (거의 사용 X)
   Type 2: Native API Driver
   Type 3: Network Protocol Driver
   Type 4: Thin Driver (순수 자바 기반, 가장 많이 사용)

```JAVA
import java.sql.*;
```

### DB연동 순서
1. 접속하려는 데이터베이스 제품 선택 (이때 각 DB에 따른 `Driver ~.jar` 찾는다)
2. 연결 - 데이터베이스의 계정으로 접속하는 것.(url, user, password 필요)
3. 실행 -  CRUD 문장 실행(DDL, DML)


4)닫기  -사용된 객체를 반납( close한다)
-------------------------------------------------------------------------
 1.  로드
   `Class.forName("연결하려는드라이버명");`
    : 드라이버 명이 오류이거나 `~.jar` 가 없으면  ClassNotFonudExecption발생

    Oracle : oracle.jdbc.driver.OracleDriver 
    Ms_Sql : sun.jdbc.odbc.JdbcOdbcDriver 
    My_Sql : com.mysql.cj.jdbc.Driver
    
2. 연결
```JAVA
Connection con = DriverManager.getConnection(String url, String user  , String pass);
```

```JAVA
-- url종류
jdbc:oracle:thin:@localhost:1521:SID
jdbc:odbc:odbc설정을통해만든db원본명 
jdbc:mysql://localhost:3306/db명 
```

```JAVA
# MY_SQL
jdbc:mysql://localhost:3306/cloud_db?serverTimezone=UTC
jdbc:mysql://localhost:3306/cloud_db?serverTimezone=Asia/Seoul
```

3.   실행  - CRUD작업 
    1. `Statement` 방식
    2. `PreparedStatement` 방식 - 권장 (Statement를 상속), 보안
	    1. SQLInjection 공격 방어

- 1의 경우 - Statement 방식
```JAVA
Statement st = con.createStatement( );
```

- DDL, DML인 경우
```JAVA
int result = st.executeUpdate(String sql);
// 결과 result가 0이면 실패, 1 이상이면 성공
// 제약 조건 위배, 길이 제한, 쿼리 오류이면 SQLExeception 발생
```

- select 전용
```JAVA
ResultSet rs = st.executeQuery(String sql);
// ex)  ResultSet rs = st.executeQuery("select empno, ename, hiredate from emp");
// boolean b = rs.next(); // 커서를 한 행 앞으로 이동

List<Emp> list = new ArrayList<>();
	while(rs.next()) { // 앞으로 커서가 이동한다면
	// 열을 이동하면서 데이터 조회 - getType(컬럼명), getType(컬럼index)
		int empno =  rs.getInt("empno"); // 1
		String ename = rs.getString("ename"); // 2
		String hiredate = rs.getString(3); // 3
		
		Emp emp = new Emp(empno, enam, sal);
		list.add(emp);
	}
```

- 2의 경우 - PreparedStatement 방식
```java
    PreparedStatement ps = con.prepareStatement(String sql);
    // ex)String sql ="insert into emp(empno, ename, job, sal, hiredate) values(?,?,?,?,now())";
          PreparedStatement ps = con.prepareStatement(sql);
          // ?의 개수만큼 순서대로 ps.setType(index, value) 설정이 필요하다.
          ps.setInt(1, emp.getEmpNo() );
          ps.setString(2, emp.getEname() );
          ps.setString(3, emp.getJob() );
          ps.setInt(4, emp.getSal() );

          int result = ps.executeUpdate( );
          
          // select인 경우
          // ex)  String sql ="select * from emp";
          PreparedStatement ps = con.prepareStatement(sql);
          ResultSet rs = ps.executeQuery();
```      

 5. 닫기
	 1. 사용한 객체를 닫는다.
```java
// ex) Connection =, Statement(PreparedStatement), ResultSet
//           ~.close(); 사용
```

```java
List<Emp> list = new ArrayList<>();

ResultSet rs = st.executeQuery("select ... from emp");

while (rs.next()) { // rs.next(): 커서를 앞으로 한 칸 이동할 수 있다면 내려가기(결과는 boolean형)
	int empno = rs.getInt(1);
	String ename = rs.getString(2);
	int sal = rs.getInt("sal"); // getInt(3); 사용도 가능
	String date = rs.getString(4);
	
	Emp e = new Emp(empno, ename, sal, date);
	list.add(e);
}
```





**DbManager.java(로드, 연결, 닫기)**
```java
class DbManager {
	static {
		Class.forName("연결하려는 driver명");
	}
	
	// 연결
	public static Connection getConnection() {
		return DriverManager.getConnection(url, id, pwd);
	}
	
	// 닫기
	public static void dbClose(Connection, PRe...., Res...) {
		~.close();
	}
}
```

**XxxDAO(실행)**
```java
// 검색
public List<Emp> selectAll() {
	try {
		Connection con = DbManager.getConnection();
		
		PreparedStatement ps = con.prexxx(sql);
		ps.executeUpdate();
		ps.executeQuery();
	} catch(Xxx e) {
	
	} finally { // 닫기
		DbManager.dbClose(con, ps, rs);
	}
}
```


# SQL Injection공격

**MySQ과 Oracle은 타입 변환 규칙 달라서 MySQL 은 예외가 나지 않는 것**

1. **PreparedStatement는 값과 SQL을 분리**
    
    `where sal > ?` 에서 `?`는 **SQL 구문이 아니라 값**으로 전달된다.
    
    즉 `"2000 or 1=1"`은 SQL로 해석되지 않고 **그냥 문자열 값**
    
2. **Oracle vs MySQL의 암묵적 타입 변환 차이**
    
    - Oracle: 숫자 비교(`sal > ?`)에 문자열이 들어오면 **문자열 전체를 NUMBER로 변환**하려고 시도합니다.
        
        `"2000 or 1=1"` → 숫자로 변환 불가 → `ORA-01722: invalid number` 예외.
        
    - MySQL: 숫자 컨텍스트에서 문자열을 보면 **앞부분의 숫자만 읽어 숫자로 변환**하고 뒤의 “쓰레기”는 무시하는, 매우 관대한 규칙을 쓴다
        
        `"2000 or 1=1"` → **2000으로 변환** → `sal > 2000`으로 평가 → 예외 없이 정상 실행.
        
        (이때 드라이버/모드에 따라 “Truncated incorrect DOUBLE value …” 같은 **경고**가 생길 수 있지만 보통 **에러는 아닙니다**.)
        
    
    즉, **인젝션은 실패**했지만(MySQL이 `or 1=1`을 코드로 보지 않음), **형 변환이 관대**해서 예외가 안 난 것이다.
    
3. 올바른 타입으로 바인딩 → 타입을 맞춰 바인딩
```java
int cutoff = Integer.parseInt(param.trim()); // 숫자 아님 -> NumberFormatException
ps.setInt(1, cutoff);
```
- DB마다 다른 암묵적 변환(Oracle의 예외, MySQL의 관대함)에 의존하지 않게 되고, **의도도 더 명확해진다.**
