**Java** → **객체 지향 언어** 
### ❗**객체 지향 언어는 무엇인가**
- *(객체 지향 언어에 대한 정의 메모하기)*

- 왜 사용할까? : 자주 사용하는 객체를 가져다가 사용, 라이브러리(API)
- **JVM** : 어느 환경에서든 실행할 수 있음
- **객체(오브젝트)** → **클래스(Java)** → 붕어빵
- **속성** → **필드(Java) →** 붕어빵을 위한 재료들: 우유, 팥
- **기능** → **메소드(Java)(**왜 만들었는지, 무슨 역할을 하는지) → 먹기 위해서

- Java는 대소문자 구분이 되기 때문에 낙타 표기법 사용

- 첫 글자가 대문자로 시작할 때 → 클래스
	- 첫 글자가 소문자로 시작할 때 → 필드
	- 첫 글자가 소문자로 시작하면서 괄호가 있을 때 → 메소드

- **final** : 상수(변경이 안되는 고정 값) → 사용자 정의에 따라서 고정됨
```java

/*
class이름의 첫 글자는 대문자로 시작한다.
method이름, 변수이름의 첫 글자 소문자로 시작한다.
예외: 상수는 대문자로 시작
첫 번째 글자는 A~Z, a~z, _ , $ 한글 가능(숫자 사용X)
두 번째 글자부터 숫자 사용가능
한글 사용가능, _ , $ 이외의 특수문자 사용 안됨.
method는 항상 ( ) 붙는다.
길이제한 없음, 예약어(keyword) 사용안됨.
Unicode 지원가능 - 2byte

두단어의 합성으로 이름이 명명될 경우, 두번째 단어의 첫글자를 대문자로 하고 constant는 _을 이용하여 두 단어를 붙이게한다. 즉, 단어와 단어가 조합될때 낙타표기법(Camel)사용한다. 단, 상수는 모두 대문자로 작성한다. : 단어와 단어가 조합될때 _ (언더바)를 사용한다.
*/
String userName = "도연"; // 변수 선언
userName = ""; // 변경 가능

// final 
final String USER_NAME = "도연"; // 상수
USER_NAME = ""; // 변경 불가능
```

- **static final** : 상수, 누구한테나 고정된 상수(ex: 파이의 3.14)

##### Java는 **컴파일 언어**이면서 **인터프리터** **언어처럼** 동작한다.
###### 특징
- 자바는 **한 번 컴파일한다.**
- 하지만 **기계어(.exe)로 바로 컴파일하지 않는다.**
- 대신 **바이트코드(.class)** 로 변환
- 이 바이트코드를 **JVM이 해석(인터프리트)하며 실행**

> 자바는 소스 코드를 바이트코드로 컴파일하고,
> JVM에서 인터프리트(Just In Time 포함) 방식으로 실행되는 언어이다.

##### Servlet
##### JSP

##### 리팩토링

##### MVC 패턴
- **Model**
	- 
- **View**
	- 
- **Controller**
	- *(정의)*

##### Spring Framework
> 다양한 디자인 패턴과...


C: -> Program Files -> Java... -> bin : 실행 파일이 모여있는 폴더

- **Immutable Collection**
    - 불변 List 생성 가능( List.of() )
    - new Array : 값 변경 가능

- 매개 변수란?
	- : ~~~~


```java
[@SafeVarargs]
static <E> List<E> of(E... elements)
```

##### **지역 변수 타입 추론 (`var`)**
```java
// 컴파일러가 변수의 타입을 ‘초기값을 보고’ 자동으로 추론해주는 기능(단, Java가 동적 타입 언어가 되는 것은 아니다)
var map = new HashMap<String, Student>();
```

##### C:\Edu\Java 주소에서 cmd 검색
```java
// HelloTest.java
class HelloTest {

   // 시작을 위한 메인 메소드 작성
   public static void main(String[] args) {
         // 화면에 출력
         System.out.println("안녕하세요.");
         System.out.print("반갑습니다.");
   }
}
```

##### Project: step01_Basic > Package: ex0203 > Class : HelloTest.java
```java
package ex0203;

public class HelloTest {

	public static void main(String[] args) {

	// 모니터 출력
	// sysout ctrl + space 하면 일일이 입력하지 않아도 됨
	System.out.println("배고파요~~");
	System.out.println("곧 밥 먹으러 고고");
	
	// 한 줄 주석
	/* 여러 줄의 주석을 추가할 때
	이렇게 길게 쓸 수 있어요~~*/
	}
	
	/**
	* Document 주석
	**/
	/* public static void test(int i) {
	} */
}
```


##### 리터럴(Literal)
: 할당되어지는 값

1. 산술 연산자
```java
   5 / 2 = 2 // 같은 타입으로 연산하면 결과는 같은 타입
   5 / 2.0 = 2.5 // 서로 다른 타입으로 연산하면 결과는 큰 타입
   // int(5)는 4 바이트, double(2.0)은 8 바이트기 때문에 double 타입으로 출력
```

```java
int i = 5, j = 5;
if (i == j) // 값 비교
// -------------------------------------
String s1 = "java", s2 = "java";

if(s1 == s2) // 값 비교 X, 주소 값 비교
if(s1.equals(s2))....// 문자열 비교
```

#### 타입 변환(형 변환)
1. Promotion
	1. 작은 타입을 큰 타입으로 변환
	2. 자동 변환
2. Casting
	1. 큰 타입을 작은 타입으로 변환
	2. Casting 연산자 필요
		1. 작은 허용 범위 타입 = (작은 허용 범위 타입) 큰 허용 범위 타입;
```java
// int -> byte 강제 타입 변환
int intValue = 10;
byte byteValue= intValue; // error
```
##### 타입 별 크기 순서
- byte(1 byte) < short, char(2 byte) < int(4 byte) < long(8 byte) < float(4 byte) < double(8 byte)


- 숫자(int) <-> 문자열(String) : Casting 한다고 표현하지 X

```java

```


![[Pasted image 20260203155256.png]]
> 프로젝트 오른쪽 마우스 버튼 -> Build Path -> Configure Build Path -> Java Compiler
> Java 버전을 바꿔서 테스트하고 싶을 때

```java
package ex0203.제어문;

public class SwitchExam01 {
	public static void main(String[] args) {
	// 1~12 사이의 난수를 발생한다.
	// 그 난수는 월이라고 생각하고, 월에 해당하는 마지막 일수를 출력한다.
	double nansu = (Math.random() * 12 + 1); // random() * 경우의 수 + 최솟값
	System.out.println("난수 = " + nansu);

	int month = (int)nansu;
	System.out.println("변환 = " + month);
	
	int endDay = 31;
	
	// if 문
	if(month == 2) endDay = 28;
	else if(month == 4 || month == 6 || month == 9 || month == 12) endDay = 30;
	else endDay = 31;
	// ------------------------------------------------------
	// switch 문
	switch(month) {
		case 2 : endDay = 28; break;
		case 4 : case 6 : case 9 : case 11 : endDay = 30; break;
	}
	// ------------------------------------------------------
	endDay = switch(month) {
		case 2 -> 28;
		case 4, 6, 9, 11 -> 30;
		default -> 31;
	};
	// ------------------------------------------------------
	endDay = switch(month) {
		case 2 -> 28;
		case 4, 6, 9, 11 -> {
			System.out.println("안녕");
				yield 30;
			}
			default -> 31;
		};
		System.out.println(month + "월은 " + endDay + "일까지 있습니다.");
	}
}
```
